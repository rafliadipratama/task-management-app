# Server Configuration
PORT=5001
NODE_ENV=development

# PostgreSQL Connection (Default for production / docker-compose)
DATABASE_URL="postgresql://postgres:postgrespassword@localhost:5432/task_management_db?schema=public"

# SQLite Alternative (for instant zero-dependency local development)
# DATABASE_URL="file:./dev.db"

# Client CORS Origin
CLIENT_ORIGIN=http://localhost:3000
